<Window x:Class="LSR.XmlHelper.Wpf.Views.EditSummaryExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Edit Summary"
        Height="260"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="File name (no extension needed):" />
        <TextBox Grid.Row="1" Margin="0,6,0,10" Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}" />

        <GroupBox Grid.Row="2" Header="Notes (optional)">
            <TextBox Margin="8" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="90" Content="Cancel" IsCancel="True"/>
            <Button Width="90" Margin="10,0,0,0" Content="OK" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
